═══════════════════════════════════════════════════════════════
🚨 ИСПРАВЛЕНИЕ МИГРАЦИЙ НА PRODUCTION - КОПИРУЙТЕ ЭТИ КОМАНДЫ
═══════════════════════════════════════════════════════════════

Проблема: Миграция 20260213091146_add_user_profile_fields упала

──────────────────────────────────────────────────────────────
ШАГ 1: ПОДКЛЮЧИТЕСЬ К СЕРВЕРУ
──────────────────────────────────────────────────────────────

ssh root@spb-3-vm-2g6f
cd /opt/orchestra-back

──────────────────────────────────────────────────────────────
ШАГ 2: УДАЛИТЕ FAILED МИГРАЦИИ
──────────────────────────────────────────────────────────────

docker exec -it orkestr-postgres psql -U postgres -d dophamin_orkestr -c "DELETE FROM _prisma_migrations WHERE finished_at IS NULL;"

──────────────────────────────────────────────────────────────
ШАГ 3: СОЗДАЙТЕ ТАБЛИЦЫ ВРУЧНУЮ (если их нет)
──────────────────────────────────────────────────────────────

docker exec -it orkestr-postgres psql -U postgres -d dophamin_orkestr << 'EOSQL'
CREATE TABLE IF NOT EXISTS "UserProfile" (
  "email" TEXT PRIMARY KEY,
  "displayName" TEXT,
  "firstName" TEXT,
  "lastName" TEXT,
  "telegramUsername" TEXT,
  "telegramId" TEXT UNIQUE,
  "avatarUrl" TEXT,
  "availabilityCalendar" JSONB,
  "sex" TEXT,
  "role" TEXT,
  "characters" JSONB,
  "phone" TEXT,
  "birthday" TEXT,
  "createdAt" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP
);

DO $$ BEGIN
  IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'RehearsalParticipantStatus') THEN
    CREATE TYPE "RehearsalParticipantStatus" AS ENUM ('unknown', 'present', 'absent', 'late');
  END IF;
END $$;

CREATE TABLE IF NOT EXISTS "Rehearsal" (
  "id" TEXT PRIMARY KEY,
  "projectId" TEXT NOT NULL,
  "title" TEXT NOT NULL,
  "startsAt" TIMESTAMP(3) NOT NULL,
  "durationMin" INTEGER,
  "notes" TEXT,
  "place" TEXT,
  "telegramChatId" TEXT,
  "telegramMessageId" TEXT,
  "telegramThreadId" TEXT,
  "createdAt" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
  "createdBy" TEXT,
  "createdVia" TEXT,
  FOREIGN KEY ("projectId") REFERENCES "Project"("id")
);

CREATE TABLE IF NOT EXISTS "RehearsalParticipant" (
  "id" TEXT PRIMARY KEY,
  "rehearsalId" TEXT NOT NULL,
  "email" TEXT NOT NULL,
  "telegramId" TEXT,
  "userName" TEXT,
  "status" "RehearsalParticipantStatus" DEFAULT 'unknown',
  "roles" JSONB,
  "lateTime" TEXT,
  "createdAt" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY ("rehearsalId") REFERENCES "Rehearsal"("id") ON DELETE CASCADE,
  UNIQUE ("rehearsalId", "email")
);

CREATE TABLE IF NOT EXISTS "BotSettings" (
  "id" TEXT PRIMARY KEY DEFAULT 'singleton',
  "groupChatId" TEXT,
  "attendanceThreadId" TEXT,
  "announcementsThreadId" TEXT,
  "projectSlug" TEXT,
  "createdAt" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO "BotSettings" ("id", "updatedAt") 
VALUES ('singleton', CURRENT_TIMESTAMP) 
ON CONFLICT DO NOTHING;

CREATE INDEX IF NOT EXISTS "Rehearsal_projectId_startsAt_idx" ON "Rehearsal"("projectId", "startsAt");
CREATE INDEX IF NOT EXISTS "RehearsalParticipant_email_idx" ON "RehearsalParticipant"("email");
CREATE INDEX IF NOT EXISTS "RehearsalParticipant_telegramId_idx" ON "RehearsalParticipant"("telegramId");
EOSQL

──────────────────────────────────────────────────────────────
ШАГ 4: ПЕРЕЗАПУСТИТЕ
──────────────────────────────────────────────────────────────

docker-compose restart back

──────────────────────────────────────────────────────────────
ШАГ 5: ПРОВЕРЬТЕ ЛОГИ
──────────────────────────────────────────────────────────────

docker logs orkestr-back --tail=50 -f

──────────────────────────────────────────────────────────────
ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
──────────────────────────────────────────────────────────────

✅ Telegram бот запущен
✅ Application successfully started

══════════════════════════════════════════════════════════════

Если нужна подробная информация, смотрите:
- FIX_ALL_MIGRATIONS.md
- QUICK_FIX_PRODUCTION.md
- MIGRATION_FIX.md
